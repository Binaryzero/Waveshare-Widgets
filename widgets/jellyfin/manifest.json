{
  "id": "ws.stock.jellyfin",
  "name": "Jellyfin",
  "author": "Plinth",
  "version": "1.5.2",
  "description": "Who's streaming what from your Jellyfin server and the titles that just landed in the library — or a player that browses your movie and TV libraries (genres, sorting, series → seasons → episodes), picks up what you left part-watched, queues what's next up, keeps your favourites a tab away, and streams straight to the panel. Read from Jellyfin's own API, because the web app refuses to render inside a frame.",
  "min_api_version": 1,
  "supported_slots": [
    "half",
    "full"
  ],
  "properties": [
    {
      "name": "baseUrl",
      "label": "Jellyfin address",
      "type": "text",
      "default": "",
      "placeholder": "http://192.168.1.20:8096",
      "help": "The address you open Jellyfin on, including the port — typically http://<server>:8096. Use an https:// address if you have one: every poll sends the API key with the request, and over plain http that credential crosses your network in the clear."
    },
    {
      "name": "apiKey",
      "label": "API key",
      "type": "secret",
      "placeholder": "Dashboard → API Keys",
      "help": "A Jellyfin API key: Administration → Dashboard → Advanced → API Keys → +. An API key carries server-wide rights — it can read every library and see every user's activity — so treat it like the server's password."
    },
    {
      "name": "certValidation",
      "label": "Certificate check (https)",
      "type": "select",
      "options": [
        "Require a valid certificate",
        "Allow self-signed"
      ],
      "default": "Require a valid certificate",
      "help": "Only matters for an https:// address. Every request carries the API key, so by default the certificate must prove the server is really yours before anything is sent. Allow self-signed trades that proof away — for API polling and Player-view playback alike — and only works when the address is a literal LAN IP (like https://192.168.1.20) or localhost; at a named address the certificate must be one this PC trusts. Most self-hosted servers have no signed certificate: pick Allow self-signed for those, which on the same LAN is no more exposed than the plain-http default Jellyfin ships with."
    },
    {
      "name": "userName",
      "label": "Jellyfin username",
      "type": "text",
      "default": "",
      "placeholder": "Optional — for Continue, Next up & Favorites",
      "help": "Optional. Name a Jellyfin user (Administration → Users) and the Player gains three queue tabs — Continue (everything that user left part-watched, resuming where it stopped), Next up (the next unwatched episode of every series in progress) and Favorites — and the panel starts keeping that user's watch history: the position it reaches is saved, so a title stopped midway waits in Continue on every client, and a title played to the end is marked watched. Press and hold any title to favourite it or set it watched by hand, and library tiles start showing that user's ✓ and ♥. Blank keeps panel playback untracked and hides all three tabs. The API key already holds server-wide rights; the name only chooses whose history the panel reads and writes — and with a name set, the libraries listed are the ones that user may see."
    },
    {
      "name": "view",
      "label": "View",
      "type": "select",
      "options": [
        "Server overview",
        "Player"
      ],
      "default": "Server overview",
      "help": "Server overview lists every active stream plus the recently-added shelf. Player turns the tile into a little theater: browse your movie and TV libraries by tab — sort, filter by genre, step into a series' seasons and episodes — and tap a title to play it right on the panel; an episode that ends rolls into the next one. Press and hold a title for its own sheet: favourite it, mark it watched or unwatched, or play it. Playback is untracked unless a username is set above — an API key alone belongs to the server, not to a user — and with one set, Continue, Next up and Favorites tabs appear, positions are saved as you watch, and finished titles are marked watched. Place two copies of the widget to have both views."
    },
    {
      "name": "hideLibraries",
      "label": "Hidden libraries",
      "type": "list",
      "itemLabel": "library",
      "fields": [
        {
          "key": "name",
          "label": "Library name",
          "type": "text",
          "placeholder": "Home Videos"
        }
      ],
      "default": [],
      "help": "Libraries to leave out — gone from the Player's tabs and from the recently-added shelf alike. Names match as Jellyfin shows them, any capitalization. Empty shows every library."
    },
    {
      "name": "latestTypes",
      "label": "Recently added",
      "type": "select",
      "options": [
        "Movies & episodes",
        "Movies only",
        "Episodes only",
        "Hidden"
      ],
      "default": "Movies & episodes",
      "help": "What the recently-added shelf collects. Hidden removes it everywhere — the Server overview's shelf and the Player's New tab alike; the Player then opens on Continue (when a username is set) or the first library."
    },
    {
      "name": "refreshSeconds",
      "label": "Refresh every (seconds)",
      "type": "number",
      "min": 5,
      "max": 300,
      "step": 5,
      "default": 15
    }
  ]
}
